# requirements.txt

# 핵심 의존성
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
diffusers>=0.20.0
accelerate>=0.20.0

# 이미지 처리
Pillow>=9.5.0
numpy>=1.24.0

# 데이터베이스
sqlalchemy>=2.0.0

# 유틸리티
python-dotenv>=1.0.0
tqdm>=4.65.0

# 선택적 의존성 (고급 기능)
# LoRA 훈련
peft>=0.4.0

# DRaFT+ 훈련
# trlx>=0.6.0  # 필요시 설치

# 개발 의존성
pytest>=7.3.0
black>=23.3.0
flake8>=6.0.0
mypy>=1.3.0